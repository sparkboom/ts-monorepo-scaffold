import * as api from '../Api/selectors';
import * as app from '../App/selectors';
import * as tasks from '../Tasks/selectors';
import * as config from '../config/selectors';
import * as translations from '../translations/selectors';
import * as content from '../Content/selectors';
import * as notification from '../Notification/selectors';

export { app, api, tasks, config, translations, content, notification };
